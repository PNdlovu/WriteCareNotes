# PostgreSQL Client Authentication Configuration File
# High-Availability Setup

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Docker network connections
host    all             all             172.25.0.0/16           scram-sha-256

# Replication connections
host    replication     replicator      172.25.0.0/16           scram-sha-256

# Allow connections from application instances
host    writecarenotes_production  writecarenotes  172.25.0.0/16  scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0               reject

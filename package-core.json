{
  "name": "writecarenotes-enterprise-core",
  "version": "1.0.0",
  "description": "Enterprise Core Services for WriteCareNotes Healthcare Management System",
  "scripts": {
    "build:core": "npx tsc --noEmit --skipLibCheck src/hooks/useAnalytics.ts src/routes/organization/index.ts src/services/health/HealthService.ts src/utils/logger.ts",
    "test:core": "npm run build:core && echo 'All core services compile successfully'",
    "validate:enterprise": "npm run test:core",
    "deploy:core": "npm run validate:enterprise && echo 'Core services ready for deployment'",
    "build": "npm run build:core",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "keywords": [
    "healthcare",
    "enterprise",
    "care-management",
    "nhs",
    "cqc",
    "uk-compliance"
  ],
  "author": "WriteCareNotes Enterprise Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "winston": "^3.11.0",
    "uuid": "^9.0.1",
    "@types/uuid": "^9.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}